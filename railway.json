{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "pnpm install && cd apps/api && pnpm prisma generate && pnpm prisma migrate deploy && cd ../.. && pnpm --filter api start & pnpm --filter bot start & pnpm --filter worker start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
