{
  "name": "vip-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter api dev\" \"pnpm --filter bot dev\" \"pnpm --filter worker dev\"",
    "build": "pnpm -r build",
    "migrate": "pnpm --filter api migrate",
    "migrate:dev": "pnpm --filter api migrate:dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  }
}
